# Task 4.2 — Create desktop asset files (HTML/JS/CSS)

## Objective

Create the desktop front-end asset files under
`src/Presentation/Desktop/assets/`.

## Steps

1. Create `src/Presentation/Desktop/assets/index.html` — a minimal page.
2. Create `src/Presentation/Desktop/assets/app.js` — a minimal script.
3. Create `src/Presentation/Desktop/assets/styles.css` — a minimal stylesheet.

> These are placeholders for now; they will be fleshed out in Tasks 7.x by
> reusing the Web UI assets.

## Acceptance criteria

- [ ] Three asset files exist under `src/Presentation/Desktop/assets/`.
- [ ] Project compiles.

## Verification

```bash
dotnet build -c Debug
```

Expected: `Build succeeded`.
