# Task 9.1 — Update README / help for `--desktop`

## Objective

Document the `--desktop` usage in the README and help displays.

## Steps

1. Update `README.md` / `README_FR.md` to mention the `--desktop` mode.
2. Update `HelpDisplay` / `DocDisplay` to mention `--desktop`.

## Acceptance criteria

- [ ] `--desktop` is documented in README and help.
- [ ] Project compiles.

## Verification

```bash
dotnet build -c Debug
```

Expected: `Build succeeded`.
